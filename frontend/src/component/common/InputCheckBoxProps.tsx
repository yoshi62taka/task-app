import { BaseInputCheckBoxProps } from "../../types/InputTypeProps";

const InputCheckBox = (props: BaseInputCheckBoxProps) => {
  return <input className="input-checkbox" {...props} />;
};

export default InputCheckBox;
